function findStrainTimeExtrema(time,strainHistory,y,z)

    [numEl,~] = size(strainHistory);
    numGP = length(strainHistory(1).eps_xx_0);
    
    index = 1;
    for i=2:length(time)
        for j=1:numEl
            for k=1:numGP
            strain = strainHistory(elNum,i-1);
            [eps_xx(i,index),gam_xy(i,index),gam_xz(i,index)] = calculateStrain(strain,gpNum,y,z);
            end
        end
    end
disp('test');
end